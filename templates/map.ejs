/**
 * Map interface for <%= ifaceName %> with specialized getters and setters.
 */
<% var className = ifaceName + suffix %>
export interface <%= className %> extends ImmutableMethods<<%= keyType %>, <%= indexerType %>, <%= className %>> {

    <% if (emitMarkers) { -%>
        $<%= className %>: <%= className %>
    <% } -%>

    <% iface.members.forEach(function(member) { -%>
        <%- include('member', { iface: iface, member: member, className: className }) %>
    <% }) -%>

    get(key: string, defaultValue?: any): <%= indexerType %>;
    set(key: string, value: typeof undefined): <%= className %>;

    toJS(): <%= iface.name.text %>
}
