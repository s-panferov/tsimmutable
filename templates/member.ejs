<% if (isRecord) { -%>
    <%= member.name.text %>: <%- this.typeMap(member, isRecord) %>
<% } -%>
get(key: '<%= member.name.text %>', defaultValue?: <%- this.typeMap(member, isRecord) %>): <%- this.typeMap(member, isRecord) %>
set(key: '<%= member.name.text %>', value: <%- this.typeMap(member, isRecord) %>): <%= className %>
